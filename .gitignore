# Personal answer data. The Pages workflow publishes the whole repo root, so
# anything committed here is served publicly.
answers.json
results.json
values-profile-*.json

# OS and tooling
.DS_Store
node_modules/
